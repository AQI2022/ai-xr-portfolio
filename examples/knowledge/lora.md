# LoRA 微调

本文是作品集自编测试资料。

LoRA 冻结基础模型权重，在目标线性层增加可训练的低秩矩阵。训练时只优化适配器参数，减少需要更新的参数量。QLoRA 还使用低比特基础权重加载，但量化配置、硬件兼容与优化器支持需要单独验证。

监督微调可使用指令、输入和回答组成的数据。Causal LM 的训练需要对 prompt token 设置 -100 标签，只在目标回答 token 上计算损失，并保留训练和验证数据的分离。

训练损失下降只表示对训练分布拟合更好，不证明泛化能力提高。应比较基础模型与适配器的独立验证损失、生成回答和任务指标，并记录种子、版本、超参数和训练设备。
